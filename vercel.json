{
    "buildCommand": "npm run build -- --configuration production",
    "outputDirectory": "dist/films-api/browser",
    "env": {
        "API_KEY": "@api_key",
        "API_READ_TOKEN": "@api_read_token"
    }
}